{
  "name": "logistics-management-system",
  "version": "1.0.0",
  "description": "B2B Logistics Management and Order Tracking System",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install && cd ../NEXLOG.App && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
